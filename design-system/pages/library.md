# Library Page Override

- Priority: browsing speed over decorative whitespace
- Header stays aligned with other tabs: standard title row, subtle stat chips, separate control panel
- Grid minimum width: 152px cards on desktop shells
- Cards lift slightly on hover; no bounce
- Filter trays use same shell palette as content, not separate light cards
- Empty states stay inside muted panels, never plain page text